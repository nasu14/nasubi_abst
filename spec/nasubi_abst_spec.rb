require 'spec_helper'

describe NasubiAbst do
  it 'has a version number' do
    expect(NasubiAbst::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
